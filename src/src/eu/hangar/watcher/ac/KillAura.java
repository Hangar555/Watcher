package eu.hangar.watcher.ac;

import org.bukkit.Location;
import org.bukkit.event.Listener;

public class KillAura implements Listener{
/*
 * TODO
 * TODO
 * TODO
 * TODO
 * TODO
 * TODO
 */
	/*public double leastDistance(Location lo, Location tar, int height){ 
        Location ta = tar.clone();
        float addTo = 0.1f;
        double distance = lo.distance(tar);
        for (float add = 0f; add < height;add+=addTo){
            double dis = lo.distance(ta.add(0, addTo, 0));
            if (dis < distance){
                distance = dis;
                
            }else{
                break;
            }
        }
        return distance;
    }
	
    */    
    }
 

